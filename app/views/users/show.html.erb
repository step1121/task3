<% if notice %>
<%= notice %>
<% end %>

<%= render 'shownew', user: @user %>
  
  <% @books.each do |book| %>
<div>
  <p><%= image_tag book.user.get_profile_image(100,100) %></p>
  <p><%= book.title %></p>
  <p><%= book.body %></p>
</div>
<% end %>